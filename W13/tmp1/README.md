# W13/tmp1 — W13 temp 1, the 7 September engine on the new main pipe (2026-09-08)

The engine of `REF_7SEPT` (commit `3897c0e`, the run the engineer called constructable) run
unchanged on the main pipe as the client redrew it on 2026-09-08. Made to answer one question:
did the maze come from the new main pipe or from the engine's later additions? The layout
survives the new pipe: the west settlement still converges on its south-west corner (28.2 km
against 24.0 km), sub mains 33.8 km against 34.2 km, 28 sub-networks against 30, 9 joins on
the east against 15. So the maze came from the additions, not the pipe. Rerun with
`python run_stage_a.py` from `py/`; it writes here only (`OUT` in `py/config_built.py`). No
depth report: the 7 September engine had none. QGIS group
`Claude W13 temp 1 (7 Sept engine, new main pipe)` with `tmp1 pipes by depth`.
